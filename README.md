if bad interned connection:

1. npm config set fetch-retry-mintimeout 20000
2. npm config set fetch-retry-maxtimeout 120000

To create react app:
npx create-react-app ./

To start application:
npm start

npm install react-icons
